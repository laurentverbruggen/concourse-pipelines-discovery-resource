---
platform: linux

image_resource:
  type: docker-image
  source: {repository: laurentverbruggen/concourse-pipelines-discovery-resource}

inputs:
- name: pipelines-discovery-resource

run:
  path: pipelines-discovery-resource/test/all.sh
